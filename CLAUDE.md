# プロジェクト: 2人の記念日サイト (anniversary-site)

## 概要
彼女との交際記念日を祝うWebサイト。**単発では終わらず、これから毎年(1年ごと)更新し続けて育てていくライフワーク的なプロジェクト**。将来的に何年も見返すものになる前提で設計方針を決めている。

## 技術スタック(決定理由込み)
- **素のHTML / CSS / JavaScriptのみ**。React/Next.jsは検討したが、今回は見送り。
  - 理由: 制作者(自分)はまだ学習途中で、フレームワークの学習コストとデザインへの集中を同時にやると消耗しやすい。まずは基礎で完成させることを優先。バックエンド(ゲストブック機能など)は将来的に別途本気で学ぶ機会に回す。
- ビルドツールなし。npm不要。ブラウザで直接開ける構成。
- 開発環境: VSCode + Live Server拡張。
- バージョン管理: Git + GitHub。リポジトリは既に作成・初回push済み。
- ホスティング予定: GitHub Pages。

## サイトの機能・構成(確定事項)

### 1. ランディング部分
- 2人の名前を表示。
- 「付き合ってから◯年◯ヶ月◯日」を**JavaScriptで動的計算**して表示。
  - 交際開始日を1箇所の変数に保持し、`今日の日付 - 開始日` で毎回計算する。
  - この部分は一度実装すれば、翌年以降さわる必要がない(自動で正しくなり続ける)。

### 2. 記念日アーカイブ(年ごとのミルストーン) ★このサイトの核となる機能
- **過去の記念日データは消さず蓄積していく**(上書きではなく追加していく方針)。
- 年ごとに「アイコン」を並べて表示。アイコンをタップ/クリックすると、**モーダル(ポップアップ)**でその年の内容(タイムライン・写真・手紙)が開く。
- **データ駆動設計にすること**が重要: HTMLを手書きで増やすのではなく、JavaScriptの配列(年ごとのオブジェクト)にデータを持たせ、その配列をループしてアイコン・モーダル内容を自動生成する。
  - 来年以降の更新作業 = 配列に1オブジェクト追加するだけ、で済むようにする。
  - データ構造イメージ:
    ```js
    const anniversaries = [
      {
        year: 1,
        date: "2025-08-21",
        thumbnail: "photos/year1.jpg",
        timeline: [
          { date: "...", title: "...", description: "..." },
          // ...
        ],
        photos: ["photos/year1-1.jpg", "photos/year1-2.jpg"],
        letter: "1年目に書いた手紙の本文..."
      },
      // year: 2 以降、来年ここに追加していく
    ];
    ```

### 3. アイコンのデザイン
- 「その年の代表写真のサムネイル」+「年数の数字バッジ」を組み合わせる。
- 当初はポップな見た目(通知バッジ風、差し色ピンクなど)を検討したが、後述のデザイン方針転換により、**上品な見せ方**に変更(例: 細い縁取りの円形バッジに、控えめな色で数字のみ)。

## デザイン方針(重要・方針転換あり)
- **当初案**: 「遊び心のあるポップ」なデザイン(紫メイン + コーラルピンクなどの差し色、跳ねるアニメーション等)。
- **→ 方針転換後(確定)**: 「一生更新し続けて、将来子供と見返す可能性もある」ことを考慮し、**シンプルで上品(エレガント/ミニマル)な方向**に変更。
  - ベース: 白・クリーム系の背景、余白多め。
  - アクセントカラー: **紫**(彼女の好きな色)。ただし全面に使うのではなく、見出し・区切り線・数字バッジなど「控えめだが印象に残るポイント」に絞って使用する。
  - フォント: 丸っこいポップな書体ではなく、シンプルで読みやすい書体。
  - アニメーション: 跳ねる・弾むような動きではなく、フェードイン、緩やかな拡大など**控えめで上品な動き**にする。
  - 補足: ユーザー(依頼者)が「ユーモアが欲しくなったらその都度言う」とのことなので、**基本は上品シンプル路線を守りつつ、追加で遊び心のある要素をピンポイントで足す拡張性は残しておく**こと。

## Git運用の方針
- 毎年の更新タイミングで、Gitタグ(例: `2周年版`, `3周年版`)を付けてコミットしておくと、サイトの成長record自体が記録として残る。

## 現在の進捗
- Git init / add / commit 完了。GitHubリポジトリ作成・初回push完了。
- `index.html` の基本骨組みを作成済み:
  ```html
  <!DOCTYPE html>
  <html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>2人の記念日</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>

    <section id="landing">
      <h1>〇〇 & △△</h1>
      <p id="counter">読み込み中...</p>
    </section>

    <script src="script.js"></script>
  </body>
  </html>
  ```
- `style.css`, `script.js` は空ファイルの状態。

## 次にやること(TODO)
1. `style.css` で全体の見た目(白・クリームベース + 紫アクセント)を整える。
2. `script.js` で交際日数の動的カウンター機能を実装する。
3. `anniversaries` データ配列の設計・実装。
4. データ配列からアイコンを自動生成するレンダリング処理。
5. アイコンクリックでモーダルを開閉する処理、モーダル内にその年のタイムライン/写真/手紙を表示する処理。
6. レスポンシブ対応の確認(スマホでの見え方)。
7. GitHub Pagesで公開。

## 学習方針の補足(制作者について)
- Web開発の学習をハッカソン形式のインターンで始めたばかりで、現在Progateで基礎(HTML/CSS/JS)を学習中。
- **自分の手で書いて学びたい意向が強い**。コードを丸ごと生成して渡すのではなく、説明しながら一緒に考え、本人が実装する進め方を希望している。Claude Codeで作業する際も、可能な範囲でこのスタンスを踏襲し、一方的に全部書き切るのではなく、要所で本人の理解・選択を挟みながら進めるのが望ましい。
